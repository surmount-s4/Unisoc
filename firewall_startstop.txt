I’ll quickly confirm the exact service and binary names from your compose/setup files so I can give you the shortest correct start and stop commands.

Read [](file:///home/unisoc/CUSTOM_LOGGERS/server_side/uls-detection-server/docker-compose.yml)

Yes, in short:

From the uls-detection-server folder:

1. Start firewall pipeline
sudo docker compose up -d rabbitmq timescaledb uls-server syslog-receiver

2. Stop only firewall pipeline
sudo docker compose stop syslog-receiver uls-server

3. Stop everything
sudo docker compose down

4. Check status
sudo docker compose ps

If your Docker user already has permission, you can remove sudo.
